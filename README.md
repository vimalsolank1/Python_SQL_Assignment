# Python & SQL Assignment

**Name:** Vimal Solanki

## Task 1 – Python Requests

* Fetched data from the API `https://jsonplaceholder.typicode.com/posts` using the requests library.
* Converted the JSON response into a pandas DataFrame.
* Printed all posts and analyzed the data to find distinct users, the user with the most posts, and the average word length of post titles.

## Task 2 – HTML Parser

* Parsed the given HTML text using BeautifulSoup.
* Extracted all `href` links and class values from `<a>` tags.
* Implemented a recursive Python function to parse the HTML without using BeautifulSoup.

## Task 3 – FastAPI

* Created two API endpoints using FastAPI.
* `/add` endpoint to accept two numbers and return their sum.
* `/uppercase` endpoint to convert a lowercase string into uppercase.

## Task 4 – Data Analysis

* Performed exploratory data analysis on the Global Electronics Retailer dataset using pandas.
* Checked dataset structure, data types, and missing values.
* Generated insights such as total products sold, top selling products, customers by country, and stores by country.
* Calculated product profitability using unit cost, unit price, and quantity sold.
* Identified top profitable products and least profitable products.
* Analyzed most profitable and least profitable countries.

## Task 5 – SQL (SQLite)

* Used SQLite with Python to create a students table.
* Wrote SQL queries to identify the second topper in different ranking scenarios.
* Applied SQL concepts such as ORDER BY, LIMIT, OFFSET, and DENSE_RANK().
